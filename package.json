{
  "name": "job-portal-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../backend/frontend && mkdir -p ../backend/frontend && cp -r dist ../backend/frontend",
    "start": "cd backend && npm install && node server.js"
  },
  "dependencies": {}
}